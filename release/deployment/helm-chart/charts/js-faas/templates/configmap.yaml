apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "configmap.name" . }}
data:
  entrypoint.sh: |
{{ .Files.Get "bootstrap/entrypoint.sh" | nindent 4 }}
  healthcheck.sh: |
{{ .Files.Get "bootstrap/healthcheck.sh" | nindent 4 }}
  js_faas_server.ts: |
{{ .Files.Get "bootstrap/js_faas_server.ts" | nindent 4 }}

